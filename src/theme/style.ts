import { Colors } from "../model";

const colors: Colors = {
  primaryBackground: "white",
  success: "green",
  warning: "yellow",
  error: "red",
  secondaryBackground: "grey",
  cloud: "rgba(0,0,0,0.4);",
};

export const styles = {
  colors,
};
