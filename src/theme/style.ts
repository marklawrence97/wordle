import { Colors } from "../model"

const colors: Colors = {
    primaryBackground: "white",
    success: "green",
    warning: "yellow",
    error: 'red',
    secondaryBackground: 'grey'
}

export const styles = {
    colors
}