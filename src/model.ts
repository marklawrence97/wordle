export interface AppState {
    target: string;
    currentWord: string
    currentGuess: number
}

export interface Colors {
    primaryBackground: string;
    secondaryBackground: string;
    success: string;
    error: string;
    warning: string;
}